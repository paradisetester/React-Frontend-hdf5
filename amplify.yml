version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm cache clean --force
        - npm install --legacy-peer-deps --no-package-lock
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 
